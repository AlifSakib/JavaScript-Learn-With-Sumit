
//Templet Literals helps us to use both single & Double Quote inside the Backquote
let = `Bangladesh`

//Interpolation : Normaly if we want to add variables into a string we use + operation to add them but interpolation makes this easier for us , by adding ${} we can dicalre the variable insde a string .

let country = `Bangladesh`
let city = `Dhaka`

let sentense =  `I love ${country} and i live in ${city}`
console.log(sentense)

const cars = ["Saab", "Volvo", "Bmw"]